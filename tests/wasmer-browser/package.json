{
  "type": "module",
  "devDependencies": {
    "vite": "^5.0.10"
  },
  "dependencies": {
    "@wasmer/sdk": "^0.6.0"
  },
  "scripts": {
    "build": "bash -c \"rm -f out/typst-community-hayagriva.webc\" && wasmer package build ../../wasmer -o out/typst-community-hayagriva.webc",
    "pretest": "npm run build",
    "test": "vite"
  }
}
